# Development Workflow

A guide to the typical development workflow.

*This is a placeholder document.*

## Running the App

1.  Start the dev server: `pnpm dev`.
2.  Open your browser to `http://localhost:5173`.

## Building the WASM module

-   Run `pnpm build:wasm`.
